/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["loremflickr.com", "i.ebayimg.com", "ir.ebaystatic.com"],
  },
}

module.exports = nextConfig
